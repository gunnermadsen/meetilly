import { MessageStylesDirective } from "./message-styles.directive";

describe('MessageStylesDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new MessageStylesDirective();
  //   expect(directive).toBeTruthy();
  // });
});
